_d12‍.x([["default",()=>_d12‍.o]]);function primeTo(input) {
  const result = input.filter((c, i, arr) => {
    if (c === 2 || c === 3 || c === 5) {
      return true
    }
    let head = c
    let tail = arr.slice(0, i - 1)
    return tail.filter(t => head % t === 0).length == 0
  })

  return result
}

_d12‍.d(primeTo);